# OA申请

## 基本信息

| 项 | 内容 |
| --- | --- |
| 所属系统 | OA |
| 一级目录 | 审批管理 |
| 二级目录 | OA申请 |
| 菜单路由 | `/oa/approval/applications` |
| 文档状态 | 持续迭代 |
| 最近更新 | 2026-05-05 |

## 当前需求

- 页面定位调整为统一的 OA 申请发起入口，转正申请只是其中一个申请类型。
- 页面分为两个标签页：`发起申请` 和 `数据记录`。
- 在人事分类中新增 `离职申请`，员工本人填写离职日期、离职原因和原因说明后提交审批。
- 离职申请提交后生成离职事件和申请版本，并并行触发风险确认待办；风险结论回写到离职申请记录。
- `发起申请` 按申请分类展示申请入口，入口卡片仅保留申请名称和图标，点击后打开表单抽屉，填写数据并提交审批。
- `发起申请` 不强制套用标准列表页规范，采用现代、简洁的入口型布局。
- `数据记录` 直接展示管理员视图，不再提供普通用户/管理员视角切换。
- `数据记录` 参考标准框架页，左侧展示一级分类和二级申请类型；一级分类仅负责展开折叠，点击二级申请类型后右侧才展示对应列表。

## 框架页映射

| 功能区 | 采用框架 | 设计说明 |
| --- | --- | --- |
| 发起申请 | 入口型工作台 | 按人事、行政、财务、采购、合同分类展示小型申请入口 |
| 申请表单 | 标准抽屉表单 | 根据申请类型动态渲染字段，提交后生成审批中记录 |
| 数据记录分类 | 标准左侧分类 | 分类包含一级业务分类、二级申请类型，不做数据汇总 |
| 数据记录列表 | 标准列表页 | 查询区、权限切换、表格、分页、`TableActions` 操作列 |
| 申请详情 | 审批详情抽屉 | 展示审批信息、申请摘要和审批链路，支持全屏 |
| 撤销申请 | 标准弹窗 | 管理员视图下进行中的申请支持撤销 |

## 字段与操作

| 类型 | 内容 |
| --- | --- |
| 标签页 | 发起申请、数据记录 |
| 申请分类 | 人事、行政、财务、采购、合同；一级分类下展示转正申请、请假申请、出差申请、费用报销、采购申请、用印申请等二级节点 |
| 申请类型 | 转正申请、离职申请、请假申请、出差申请、费用报销、采购申请、用印申请 |
| 表单字段 | 由申请类型配置动态生成，支持输入框、多行文本、下拉、日期区间、数字、上传 |
| 离职申请查询条件 | 姓名/员工编号/手机号、区域、部门、状态、离职原因、负责HR、离职日期起止 |
| 离职申请列表字段 | 序号、员工编号、姓名、区域、部门、岗位-职级、入职日期、申请版本、状态、离职日期、离职原因、原因说明、手机号、负责HR、提交时间、更新时间、操作 |
| 转正申请查询条件 | 姓名/员工编号/手机号、区域、部门、岗位、负责HR、状态、入职日期起止、实际转正日期起止 |
| 转正申请列表字段 | 序号、员工编号、姓名、区域、部门、岗位-职级、入职日期、预计转正日期、实际转正日期、状态、负责HR、提交时间、更新时间、操作 |
| 通用申请列表字段 | 序号、审批单号、流程名称、分类、发起人、部门、摘要、状态、结果、当前节点、发起时间、结束时间、操作 |
| 行内操作 | 详情、撤销 |
| 列表操作 | 导出 |

## 业务规则

- 申请类型状态为 `启用` 时可发起；状态为 `停用` 时入口保留但不可发起。
- 发起申请提交后生成一条 `进行中 / 处理中` 的 OA 申请记录，并自动切换到 `数据记录` 标签页。
- `数据记录` 默认没有选中具体表单时，右侧展示空状态提示。
- 一级分类节点只负责展开和折叠，不触发右侧列表查询。
- 二级申请类型节点按流程名称过滤右侧列表，例如点击 `转正申请` 展示转正申请列表。
- 点击 `离职申请` 展示离职申请专属列表，字段与离职审批单保持一致，不使用通用审批摘要表。
- 离职申请在审批中或已通过时，会进入 `离职管理` 列表并展示为 `待确认`；已驳回或已撤销不进入离职管理。
- 离职申请撤销仅允许申请人本人操作；撤销后状态更新为 `已撤销`。
- 离职申请审批流程与风险确认并行推进，风险确认不阻塞主管、HR、负责人审批。
- 离职申请审批详情中的审批信息不使用通用字段，改为展示姓名、区域、部门、岗位、职级、入职日期、离职日期、离职类型、离职原因、原因说明。
- 离职事项需要放在审批信息容器内展示，和离职申请字段属于同一个业务信息区。
- 离职事项展示当前申请版本需要完成的事情和完成情况；展示上先给出完成数和待完成数，再用单列列表展示事项、负责角色和完成情况。
- 风险确认不在 `OA申请 / 数据记录` 列表中展示。
- 离职申请被驳回或撤销时，当前申请版本作废，相关风险确认待办同步作废。
- 驳回或撤销后重新发起离职申请，会生成新的申请版本并重新触发风险确认待办。
- 左侧分类不展示数据汇总数量。
- 申请分类树采用接近系统菜单的浅色样式，保持简约、轻量。
- 转正申请列表中的实际转正日期直接展示日期，不追加 `待回写` 标记。
- 进行中的申请支持撤销；通用申请撤销后状态更新为 `已结束`，结果更新为 `已撤回`；转正申请撤销后状态更新为 `已撤销`。
- 转正申请使用转正申请详情抽屉展示员工信息和申请文件；其他申请使用通用审批详情抽屉。

## 原型实现记录

- 页面文件：`src/pages/OaApplicationPage.tsx`。
- 页面样式：`src/pages/OaApplicationPage.css`。
- 示例数据：`src/data/oaApplicationCenter.ts` 中的 `oaApplicationTypes` 和 `oaApplicationRecords`，离职申请专属数据在 `src/data/resignationManagement.ts`。
- 复用样式：`src/pages/standards/Standards.css`、`src/pages/ApprovalPages.css`、`src/pages/Page.css`。
- 菜单已调整为 `审批管理 / OA申请`。

## 待确认问题

- 是否需要将申请入口搜索接入真实筛选逻辑，当前为原型展示。
- 后续是否需要恢复普通用户视角，或细分负责 HR、区域、部门负责人等中间权限。
- 不同申请类型是否需要各自的专属表单布局，还是统一由动态字段配置生成。
- 转正申请是否继续需要接入 HR 通知转正的前置校验。

## 迭代记录

| 日期 | 版本 | 变更 |
| --- | --- | --- |
| 2026-05-05 | v1.1 | 风险确认从离职申请列表移至审批详情，按确认单据展示事项和处理详情。 |
| 2026-05-05 | v1.2 | 离职审批详情接管审批信息内容，展示离职申请字段，并在同一容器内优化展示离职事项确认。 |
| 2026-05-05 | v1.3 | 离职事项展示精简为事项和完成情况，移除执行动作、风险判定等过程字段。 |
| 2026-05-05 | v1.4 | 审批详情中的离职事项由多列卡片改为单列列表，强化扫描和聚焦。 |
| 2026-05-05 | v1.0 | 修正离职审批与风险确认为并行流程；风险确认只回显不阻塞审批。 |
| 2026-05-05 | v0.9 | 离职申请新增申请版本、风险确认回写字段，并补充风险确认待办流转规则。 |
| 2026-05-05 | v0.8 | 新增离职申请入口、单日期表单字段、离职申请专属数据记录列表和撤销流程。 |
| 2026-05-04 | v0.7 | 优化申请分类树为浅色菜单样式；转正申请列表新增序号列和导出按钮，实际转正日期去掉待回写标记。 |
| 2026-05-04 | v0.6 | 数据记录左侧分类改为仅展开折叠，移除全部节点和数量汇总；转正申请接入专属查询条件和列表字段，移除权限视角切换，操作列两按钮直接展示。 |
| 2026-05-04 | v0.5 | 发起申请入口卡片缩小为名称和图标；数据记录左侧分类增加二级申请类型节点，包含转正申请。 |
| 2026-05-04 | v0.4 | 将页面重构为统一 OA 申请入口，新增发起申请/数据记录双标签页、分类入口、动态表单、权限视角和左分类右列表记录页。 |
| 2026-05-04 | v0.3 | 根据《转正原文档》重构为员工侧转正申请功能，移除泛 OA 申请中心设计。 |
